# agent-workflows
Agentic Workflows (aw) is a tmux-based TUI that orchestrates AI workflows using yaml files. It will invoke skills and track progress on the sidebar pane, while the agent runs in the main pane.
